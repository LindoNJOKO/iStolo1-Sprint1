@model iStolo1.Models.MainViewModel

<!DOCTYPE html>
<html>
<head>
    <title>Main</title>
</head>
<body>
    <h1>Welcome to the Main Page</h1>
    <p>@Model.Message</p>

    @Html.ActionLink("Register", "Register", "Account", null, new { @class = "btn btn-primary" })
    @Html.ActionLink("Login", "Login", "Account", null, new { @class = "btn btn-primary" })

    @Html.ActionLink("View Products", "ProductView", "Home", null, new { @class = "btn btn-primary" })
</body>
</html>
